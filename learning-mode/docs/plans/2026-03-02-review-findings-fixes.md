# Review Findings Fixes Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix all inconsistencies, ambiguities, and philosophy misalignments identified in the comprehensive plugin review.

**Architecture:** All changes are to markdown skill/agent files in the learning-mode plugin. No code, no tests — purely documentation edits. Each task targets one file with all its changes grouped together.

**Tech Stack:** Markdown, Graphviz (dot) diagrams

---

### Task 1: socratic-brainstorming — add type declaration, fix Phase B language, update diagram

**Files:**
- Modify: `skills/socratic-brainstorming/SKILL.md`

**Step 1: Add Skill type declaration**

After the frontmatter closing `---` and before the `# Socratic Brainstorming` heading, add:

```markdown

**Skill type: Rigid** -- Follow this process exactly. Do not skip phases. Do not shortcut the Socratic process. The learner's understanding IS the product.
```

**Step 2: Soften Phase B language to acknowledge writing-plans' module identification**

Replace the current text at lines 209-212 (the Phase B explanation in section 6 "After the Design"):

Current:
```
**Implementation Plan (Phase B):**
- Invoke `learning-mode:writing-plans` to create the implementation plan
- Phase B is auto-generated by Claude based on the approved design. It is NOT Socratic — the learner has already made all the decisions. Phase B translates those decisions into actionable technical steps (file paths, commands, dependencies).
- Do NOT invoke any other skill. `learning-mode:writing-plans` is the next and only step.
```

Replace with:
```
**Implementation Plan (Phase B):**
- Invoke `learning-mode:writing-plans` to create the implementation plan
- Phase B translates the approved design into actionable technical steps (file paths, commands, dependencies). The plan generation itself is NOT Socratic — the learner has already made all the design decisions.
- `learning-mode:writing-plans` includes a **light Socratic module identification step** where the learner identifies which codebase modules need to change. This builds codebase awareness without reopening design decisions. For trivial projects where the modules are obvious, this step can be brief or skipped.
- Do NOT invoke any other skill. `learning-mode:writing-plans` is the next and only step.
```

**Step 3: Update the Red Flags table entry for Phase B**

Replace the Phase B row in the Red Flags table (line 236):

Current:
```
| "Phase B needs to be Socratic too" | Phase B is mechanical translation. The decisions are already made. Interrogating file paths wastes time. | Generate Phase B directly. All design thinking happened in Phase A. |
```

Replace with:
```
| "Phase B needs full Socratic treatment" | Phase B is mechanical translation of decisions already made. `writing-plans` includes a light module identification step for codebase awareness, but design decisions are settled. | Invoke `learning-mode:writing-plans` and let it handle the appropriate level of learner engagement. |
```

**Step 4: Add branch confirmation step to the Graphviz process flow**

In the Graphviz diagram (lines 48-92), add a `confirm_branch` node between `more_sections` (the "no, all done" path) and `write_doc`. The new node and edges:

Add node:
```
    confirm_branch [label="Confirm branch\n(continue here\nor new branch?)"];
```

Change the edge:
```
    more_sections -> write_doc [label="no, all done"];
```
to:
```
    more_sections -> confirm_branch [label="no, all done"];
    confirm_branch -> write_doc;
```

**Step 5: Verify changes**

Read the file back and confirm:
- Skill type declaration is present after frontmatter
- Phase B language acknowledges light Socratic touch
- Red Flags table entry is updated
- Graphviz includes confirm_branch node

**Step 6: Commit**

```bash
git add skills/socratic-brainstorming/SKILL.md
git commit -m "fix(brainstorming): add type declaration, fix Phase B language, add branch step to diagram"
```

---

### Task 2: verification-before-completion — add type declaration

**Files:**
- Modify: `skills/verification-before-completion/SKILL.md`

**Step 1: Add Skill type declaration**

After the frontmatter closing `---` and before the `# Verification Before Completion` heading, add:

```markdown

**Skill type: Rigid** -- Follow this process exactly. No shortcuts. Evidence before claims, always.
```

**Step 2: Verify the change**

Read the file back and confirm the type declaration is present.

**Step 3: Commit**

```bash
git add skills/verification-before-completion/SKILL.md
git commit -m "fix(verification): add missing skill type declaration"
```

---

### Task 3: evaluating-code-review — fix title, restructure presentation approach

**Files:**
- Modify: `skills/evaluating-code-review/SKILL.md`

**Step 1: Fix the internal title**

Change line 7:

Current:
```
# Receiving Code Review: Teaching Critical Evaluation of Feedback
```

Replace with:
```
# Evaluating Code Review: Teaching Critical Evaluation of Feedback
```

**Step 2: Restructure Part 1 Step 1 — hierarchical presentation instead of full dump**

Replace the current Step 1 "Present the Review" section (lines 106-114):

Current:
```
### Step 1: Present the Review

Present the full review feedback to the learner. Then work through comments one at a time.

For each comment, present:
- The reviewer's observation or concern
- The specific code or area it refers to
- The severity the reviewer assigned (if any)

**Do NOT** editorialize. Do NOT hint at whether the comment is valid. Present it neutrally.
```

Replace with:
```
### Step 1: Present the Review — Top-Down, Not All at Once

**Do NOT dump the entire review on the learner.** Present it hierarchically, starting at the highest level of abstraction and zooming in.

**Level 1 — Summary overview:** Present a brief summary of the review at the category level. Example: "The review came back with feedback in three areas: error handling, test coverage, and API design. Which area would you like to look at first?"

**Level 2 — Area walkthrough:** When the learner picks an area (or you guide them to the next one), present the comments in that area one at a time — not all at once.

**Level 3 — Individual comment:** For each comment, present:
- The reviewer's observation or concern
- The specific code or area it refers to
- The severity the reviewer assigned (if any)

**The learner drives the order.** Let them choose which area to explore first based on their interest. This builds prioritization instincts. But ensure every area gets covered before moving to Part 2 — it remains the mentor's job to make sure nothing is skipped.

**Do NOT** editorialize. Do NOT hint at whether comments are valid. Present them neutrally.
```

**Step 3: Update the process flow diagram to reflect hierarchical presentation**

In the Graphviz diagram, update the `p1_present` node label:

Current:
```
p1_present [label="Present review feedback\nto the learner\n(one comment at a time)" shape=box];
```

Replace with:
```
p1_present [label="Present review summary\n(by area, learner\nchooses order)" shape=box];
```

**Step 4: Verify changes**

Read the file back and confirm:
- Title says "Evaluating Code Review"
- Step 1 describes hierarchical presentation
- Diagram node label updated

**Step 5: Commit**

```bash
git add skills/evaluating-code-review/SKILL.md
git commit -m "fix(evaluating-code-review): fix title, restructure review presentation to be hierarchical"
```

---

### Task 4: executing-plans — remove reclassification, define orchestrator, add classification summary, lighten documentation step

**Files:**
- Modify: `skills/executing-plans/SKILL.md`

**Step 1: Add orchestrator definition**

After the "Two execution modes:" paragraph and before "Refer to..." (around line 18), add a paragraph:

```
The **orchestrator** is the primary Claude agent running this skill — the main session the learner interacts with. It coordinates subagents, manages the task flow, and conducts all Socratic interactions with the learner.
```

**Step 2: Remove reclassification from Step 2 (AUTO Batches)**

Delete the reclassification paragraph at lines 163-164:

```
**Reclassification during execution:** If the reviewer's findings reveal unexpected ambiguity or a risky autonomous decision on a task you classified as AUTO, pull that task out and run it through the REVIEW flow (Step 3) before committing.
```

Also remove the reclassification red flag from the Red Flags table:

```
| "Reclassify on the fly" entries or references
```

And update the Key Principles section — change:
```
- **Reclassify on the fly.** If an AUTO task produces surprising output, upgrade it. If a REVIEW task is trivially mechanical, move through it quickly.
```
to:
```
- **Move through trivial REVIEW tasks quickly.** If a REVIEW task turns out to be straightforwardly correct, acknowledge and move on. Don't manufacture problems.
```

**Step 3: Add lightweight classification summary to Step 1**

Replace the instruction at lines 109-110:

Current:
```
Read the implementation plan. Classify each task internally. **Do NOT present the classification to the learner.** There is no upfront confirmation step. You start executing immediately after classification.
```

Replace with:
```
Read the implementation plan. Classify each task internally.

**Present a brief summary to the learner before execution begins.** Keep it lightweight — one or two sentences, not a detailed justification per task. Example:

> "I've read through the plan. I'll handle tasks 1-2, 4-6, and 8-10 automatically — they're straightforward scaffolding and wiring. I'll pause for us to discuss tasks 3, 7, and 11 together since they involve [brief reason]. Sound good, or would you like to discuss any others too?"

The learner can simply say "OK" or add tasks they want to review. This gives them visibility without creating overhead. Do NOT present a detailed classification table or justify each decision — a quick summary with the option to adjust is sufficient.
```

Also update the red flag for this:

Current:
```
| "I should present the full classification upfront" | The learner does not need to see or approve the classification. That is overhead, not value. | Classify internally, execute, surface REVIEW tasks one at a time as they arrive. |
```

Replace with:
```
| "I should present a detailed classification justification" | A detailed per-task justification is overhead. A quick summary with the option to adjust is sufficient. | Brief summary: "I'll auto-handle X, Y, Z and we'll discuss A, B, C. Sound good?" |
```

**Step 4: Make documentation step lightly Socratic**

Replace the documentation step text at lines 253-267:

Current:
```
## Step 6: Documentation Update

After verification passes, update project documentation to reflect what was built. This step is **not Socratic** -- the orchestrator handles it directly.
```

Replace with:
```
## Step 6: Documentation Update

After verification passes, update project documentation to reflect what was built.

**Light Socratic touch:** Before writing documentation, briefly present the plan to the learner:

> "Here's what I'm planning to document: [list]. And here's what I think doesn't need documentation updates: [list with brief reasons]. What do you think?"

The learner can agree or adjust. This builds documentation judgment without making the step heavy. After agreement, the orchestrator writes the documentation directly.
```

Remove the sentence "This step is **not Socratic** -- the orchestrator handles it directly." from anywhere else it might appear.

**Step 5: Verify changes**

Read the file back and confirm:
- Orchestrator is defined
- Reclassification concept is removed
- Classification summary is present
- Documentation step has light Socratic touch

**Step 6: Commit**

```bash
git add skills/executing-plans/SKILL.md
git commit -m "fix(executing-plans): define orchestrator, remove reclassification, add classification summary, lighten docs step"
```

---

### Task 5: socratic-debugging — accept subagent output, default to all bugs

**Files:**
- Modify: `skills/socratic-debugging/SKILL.md`

**Step 1: Add subagent context input to Phase 1**

After the Phase 1 header "This phase is completely invisible to the learner..." section (around line 92-95), add a paragraph:

```
### Receiving Context from Other Skills

When triggered during `learning-mode:executing-plans` (or any subagent flow), the orchestrator may pass along failure output from the subagent — error messages, stack traces, test failure logs. **Treat this as symptom input for Phase 1, not as a completed investigation.** Subagent output describes WHAT failed, not necessarily WHERE the root cause lives or WHY. Use it as a starting point for your investigation steps, but do not skip the systematic trace-and-identify process.
```

**Step 2: Update the "too many bugs" rationalization**

Replace the row in the Rationalization Table (around line 236):

Current:
```
| "There are too many bugs to teach all of them" | Teach one thoroughly. Fix the rest quickly. One deep investigation teaches more than five shallow ones. |
```

Replace with:
```
| "There are too many bugs to teach all of them" | In typical development, you encounter one or two bugs at a time. Walk through all of them. If you genuinely face a large batch (5+), teach each one but adjust depth — spend more time on bugs with distinct failure modes and less on bugs that repeat a pattern the learner has already demonstrated understanding of. |
```

**Step 3: Verify changes**

Read the file back and confirm:
- Subagent context input section is present
- Rationalization table updated for multiple bugs

**Step 4: Commit**

```bash
git add skills/socratic-debugging/SKILL.md
git commit -m "fix(debugging): accept subagent output as symptom input, default to teaching all bugs"
```

---

### Task 6: test-driven-development — remove advanced learners variant, clarify Delete Means Delete audience

**Files:**
- Modify: `skills/test-driven-development/SKILL.md`

**Step 1: Remove "For Advanced Learners" section**

Delete the entire "For Advanced Learners" subsection (lines 174-181):

```
### For Advanced Learners

If appropriate for the learner's level, instead of presenting the test code:

1. Describe the behavior that needs testing in plain English
2. Ask the learner to write the test themselves
3. Compare their test to yours -- discuss differences in approach, coverage, and edge cases
4. This is the highest-value version of the review because the learner must reason about both the business rule AND the testing strategy
```

**Step 2: Clarify "Delete Means Delete" audience**

The "Strict Enforcement" and "Delete Means Delete" sections (lines 239-253) are enforcement rules for the implementing agent's TDD discipline, not Socratic teaching instructions for the learner. Add a context note before the "Strict Enforcement" heading:

```
## Strict Enforcement (Implementing Agent Discipline)

The following rules apply to the **implementing agent** (whether the learner is coding directly or a subagent is executing). These enforce TDD discipline during code production. They are NOT Socratic teaching instructions — they are process guardrails.
```

Replace the existing:
```
## Strict Enforcement
```

with the above.

**Step 3: Verify changes**

Read the file back and confirm:
- "For Advanced Learners" section is removed
- "Strict Enforcement" section has audience clarification

**Step 4: Commit**

```bash
git add skills/test-driven-development/SKILL.md
git commit -m "fix(tdd): remove advanced learners variant, clarify strict enforcement audience"
```

---

### Task 7: Final verification pass

**Step 1: Read all modified files**

Read each modified file and verify no orphaned references or broken cross-references:
- `skills/socratic-brainstorming/SKILL.md` — no reference to "generate Phase B directly" remains
- `skills/executing-plans/SKILL.md` — no remaining references to reclassification
- `skills/test-driven-development/SKILL.md` — no remaining references to "For Advanced Learners"
- `skills/evaluating-code-review/SKILL.md` — no remaining "Receiving Code Review" in title

**Step 2: Verify README.md is still accurate**

Read `README.md` and confirm all skill descriptions, types, and workflow diagram are consistent with the changes made. If any README content is now stale, update it.

**Step 3: Commit any final fixes**

If any fixes are needed from the verification pass:

```bash
git add -A
git commit -m "fix: final consistency pass after review findings fixes"
```
